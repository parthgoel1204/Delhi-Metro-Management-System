{
    "name": "dmrc-housekeeping",
    "version": "1.0.0",
    "description": "DMRC Housekeeping Full Stack Application",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}